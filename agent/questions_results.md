# Questions & Results

| # | Question | Generated SQL | Answer | Correct? | Iterations Needed | Notes |
|---|----------|----------------|--------|----------|--------------------|-------|
| Q1 | How many loads were delivered in the last full month available in the data? | _pending_ | _pending_ | _pending_ | _pending_ | _pending_ |
| Q2 | Which shipper had the highest total book price? | `SELECT ds.shipper_name, SUM(fl.book_price) AS total_book_price FROM analytics.fact_loads fl JOIN analytics.dim_shipper ds ON fl.shipper_id = ds.shipper_id GROUP BY ds.shipper_name ORDER BY total_book_price DESC LIMIT 1;` | Shipper 1249 — $1,915,694.16 | Yes | 1 | |
| Q3 | What is the average book price per load by pickup state? | `SELECT l.state AS pickup_state, AVG(f.book_price) AS avg_book_price FROM analytics.fact_loads f JOIN analytics.dim_lane ln ON f.lane_id = ln.lane_id JOIN analytics.dim_location l ON ln.source_location_id = l.location_id GROUP BY l.state ORDER BY avg_book_price DESC;` | 44 states, ranging from SC ($2,708.99) down to VT ($0.00) | Yes | 1 |  |
| Q4 | What are the top 5 lanes by number of delivered loads? | `SELECT dl.lane_name, COUNT(*) AS delivered_loads FROM analytics.fact_loads fl JOIN analytics.dim_lane dl ON fl.lane_id = dl.lane_id WHERE fl.delivery_date IS NOT NULL AND (fl.load_was_cancelled IS NULL OR fl.load_was_cancelled = FALSE) GROUP BY dl.lane_name ORDER BY delivered_loads DESC LIMIT 5;` | 1. Hawkins,TX → Roanoke,TX (882) 2. Lodi,CA → Pacific,WA (150) 3. Kent,WA → Spokane,WA (94) 4. Henderson,NV → Tracy,CA (87) 5. Taft,CA → Tracy,CA (72) | Yes | 1 | |
| Q5 | Which carrier moved the most loads into Texas? | `SELECT c.carrier_name, COUNT(*) AS load_count FROM analytics.fact_loads f JOIN analytics.dim_carrier c ON f.carrier_id = c.carrier_id JOIN analytics.dim_lane l ON f.lane_id = l.lane_id JOIN analytics.dim_location loc ON l.target_location_id = loc.location_id WHERE loc.state = 'TX' GROUP BY c.carrier_name ORDER BY load_count DESC LIMIT 1;` | Carrier 567581 — 188 loads | Yes | 1 | |
| Q6 | How does the average book price compare between intrastate and interstate loads? | `SELECT CASE WHEN o.state = d.state THEN 'Intrastate' ELSE 'Interstate' END AS load_type, COUNT(*) AS total_loads, AVG(f.book_price) AS avg_book_price FROM analytics.fact_loads f JOIN analytics.dim_lane l ON f.lane_id = l.lane_id JOIN analytics.dim_location o ON l.source_location_id = o.location_id JOIN analytics.dim_location d ON l.target_location_id = d.location_id GROUP BY CASE WHEN o.state = d.state THEN 'Intrastate' ELSE 'Interstate' END;` | Interstate: $1,709.82 (n=3,534) — Intrastate: $584.49 (n=1,821) | Yes | 1 | |
| Q7 | For the shipper with the most delivered loads, how did monthly volume change across the period covered by the data? | `WITH delivered_loads AS (SELECT shipper_id, delivery_date FROM analytics.fact_loads WHERE delivery_date IS NOT NULL AND (load_was_cancelled IS NOT TRUE)), top_shipper AS (SELECT shipper_id FROM delivered_loads GROUP BY shipper_id ORDER BY COUNT(*) DESC LIMIT 1) SELECT DATE_TRUNC('month', dl.delivery_date) AS delivery_month, COUNT(*) AS delivered_load_count FROM delivered_loads dl JOIN top_shipper ts ON dl.shipper_id = ts.shipper_id GROUP BY DATE_TRUNC('month', dl.delivery_date) ORDER BY delivery_month;` | Shipper 758 — monthly volume rises from 127 (Jan/24) to a peak of 192 (Nov/24), then drops to 116 (Dec/24), 40 (Jan/25), 0 (Feb/25), 1 (Mar/25) | Yes | 1 | |
| Q8 | Among lanes with at least 10 delivered loads, which had the highest average book price? | `SELECT dl.lane_name, COUNT(*) AS delivered_load_count, AVG(fl.book_price) AS avg_book_price FROM analytics.fact_loads fl JOIN analytics.dim_lane dl ON fl.lane_id = dl.lane_id WHERE fl.delivery_date IS NOT NULL AND (fl.load_was_cancelled IS FALSE OR fl.load_was_cancelled IS NULL) GROUP BY dl.lane_name HAVING COUNT(*) >= 10 ORDER BY avg_book_price DESC LIMIT 1;` | Stockton,CA → Parrish,FL — 11 delivered loads, avg $6,800.00 | Yes | 1 | |
| Q9 (custom) | What is the overall on-time delivery rate? | `SELECT SUM(CASE WHEN carrier_on_time_to_delivery THEN 1 ELSE 0 END)::numeric / NULLIF(COUNT(*), 0) AS on_time_delivery_rate FROM analytics.fact_loads WHERE load_was_cancelled = false;` | 91.24% | Yes | 1 | |
| Q10 (custom) | What is the average price per mile by equipment type? | `SELECT equipment_type, AVG(book_price / mileage) AS avg_price_per_mile FROM analytics.fact_loads WHERE (load_was_cancelled IS NOT TRUE) AND mileage IS NOT NULL AND mileage > 0 GROUP BY equipment_type;` | FBE: $10.08/mi (n=9) — RFR: $3.77/mi (n=880) — DRV: $3.34/mi (n=3,907) | Yes | 1 | |
| Q11 (cannot answer) | Why was load 206690321 cancelled? | `SELECT loadsmart_id, load_was_cancelled FROM analytics.fact_loads WHERE loadsmart_id = 206690321;` | Confirms `load_was_cancelled = True`; no reason available | No | No | Data gap, not fixable via schema.yml or modeling, no `cancellation_reason` column exists anywhere in `fact_loads`/`stg_loads`/the raw source. Would need the source system to start send a reason at cancellation time. |
| Q12 (bonus) | What is the average delivery time? | `SELECT AVG(delivery_date - book_date) AS average_delivery_time FROM analytics.fact_loads WHERE delivery_date IS NOT NULL AND book_date IS NOT NULL;` | (6 days, 4.2h) | Yes | 2 | Assumption: "delivery time" = `delivery_date - book_date`, documented in schema.yml on `book_date`. First run used `pickup_date` instead; after the schema.yml note, the agent consistently uses `book_date`.
